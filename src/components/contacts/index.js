import React, { Component } from 'react';

import './style.css';

export default class Contacts extends Component {

    render() {
        return (
            <div>
                <h2>contacts</h2>
            </div>
        );
    }
}